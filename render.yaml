services:
  # Backend Web Service
  - type: web
    name: finance-dashboard-api
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn core.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: finance-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*" # Change this to your domain later
      - key: PYTHON_VERSION
        value: 3.12.9

  # Frontend Static Site
  - type: static
    name: finance-dashboard-ui
    runtime: node
    buildCommand: "npm run build"
    publishPath: "dist"
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: finance-dashboard-api
          property: host
        # Prepend https:// to the host provided by Render
        importValue: "https://finance-dashboard-api.onrender.com"

databases:
  - name: finance-db
    plan: free # Use "starter" for production persistence or "free" for testing
